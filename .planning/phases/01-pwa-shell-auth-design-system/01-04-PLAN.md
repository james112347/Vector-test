---
phase: 01-pwa-shell-auth-design-system
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - src/pages/Login.tsx
  - src/pages/Register.tsx
  - src/pages/Terms.tsx
  - src/pages/Dashboard.tsx
  - src/routes/ProtectedRoute.tsx
  - src/routes/router.tsx
  - src/components/layout/AppShell.tsx
  - src/components/layout/Header.tsx
  - src/App.tsx
  - src/main.tsx
autonomous: false

must_haves:
  truths:
    - "Unauthenticated users see only the login page -- no app content is accessible"
    - "User can register with email, password, and must check T&C checkbox (unchecked by default)"
    - "Registration fails if T&C checkbox is not checked"
    - "T&C text is viewable on a separate page linked from the registration form"
    - "After login, user sees the Dashboard page inside the AppShell layout"
    - "User session persists: closing and reopening the browser keeps user logged in"
    - "User can log out from the header, which returns them to the login page"
    - "Dark mode toggle in the header switches between light and dark themes"
    - "Dark mode preference persists across page reloads"
    - "App shell layout is mobile-first with responsive design"
  artifacts:
    - path: "src/pages/Login.tsx"
      provides: "Login form with email/password fields and link to register"
      contains: "signIn"
    - path: "src/pages/Register.tsx"
      provides: "Registration form with email, password, T&C checkbox, and link to terms"
      contains: "hasAcceptedTerms"
    - path: "src/pages/Terms.tsx"
      provides: "Terms and Conditions page with placeholder legal text"
    - path: "src/pages/Dashboard.tsx"
      provides: "Placeholder dashboard page shown after login"
    - path: "src/routes/ProtectedRoute.tsx"
      provides: "Auth guard that redirects to /login if not signed in"
      exports: ["ProtectedRoute"]
    - path: "src/routes/router.tsx"
      provides: "React Router v7 configuration with public and protected routes"
      exports: ["router"]
    - path: "src/components/layout/AppShell.tsx"
      provides: "Main layout wrapper with header for authenticated pages"
      exports: ["AppShell"]
    - path: "src/components/layout/Header.tsx"
      provides: "App header with dark mode toggle and logout button"
      exports: ["Header"]
  key_links:
    - from: "src/routes/ProtectedRoute.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuthState for isSignedIn check"
      pattern: "useAuthState|useAuth"
    - from: "src/routes/router.tsx"
      to: "src/routes/ProtectedRoute.tsx"
      via: "ProtectedRoute wraps authenticated routes"
      pattern: "ProtectedRoute"
    - from: "src/pages/Login.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuthActions().signIn"
      pattern: "signIn"
    - from: "src/pages/Register.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuthActions().register with T&C flag"
      pattern: "register.*acceptedTerms|hasAcceptedTerms"
    - from: "src/components/layout/Header.tsx"
      to: "src/lib/useDarkMode.ts"
      via: "useDarkMode hook for toggle"
      pattern: "useDarkMode"
    - from: "src/components/layout/Header.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "useAuthActions().signOut"
      pattern: "signOut"
    - from: "src/App.tsx"
      to: "src/contexts/AuthContext.tsx"
      via: "AuthProvider wraps RouterProvider"
      pattern: "AuthProvider"
---

<objective>
Build the complete auth UI flow (Login, Register with T&C, Terms page), the protected routing system, the app shell layout with header (dark mode toggle + logout), and integrate everything into the main App component.

Purpose: This is the "glue" plan that connects the design system (Plan 02) and auth engine (Plan 03) into a working user-facing application. After this plan, a user can install the PWA, register, accept T&C, log in, see the dashboard, toggle dark mode, log out, and return to the login wall.
Output: Complete auth flow, protected routing, app shell layout, and a fully integrated App.tsx.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pwa-shell-auth-design-system/01-RESEARCH.md
@.planning/phases/01-pwa-shell-auth-design-system/01-02-SUMMARY.md
@.planning/phases/01-pwa-shell-auth-design-system/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create auth pages (Login, Register, Terms) with form validation</name>
  <files>
    src/pages/Login.tsx
    src/pages/Register.tsx
    src/pages/Terms.tsx
  </files>
  <action>
    1. Create `src/pages/Login.tsx`:
       - Use Card component as the form container
       - Fields: email (Input, type="email", required), password (Input, type="password", required)
       - Submit button (Button component) calls `useAuthActions().signIn(email, password)`
       - Show error message on invalid credentials (catch error from signIn, display in red text)
       - Include a loading state on the button during sign-in
       - Link to `/register` for new users: "Don't have an account? Register"
       - Center the card on the page with `min-h-screen flex items-center justify-center`
       - Use the design token colors: bg-background, text-foreground, etc.
       - Mobile-first: card takes full width on mobile (max-w-md on larger screens)
       - Form uses `onSubmit` with `e.preventDefault()`

    2. Create `src/pages/Register.tsx`:
       - Use Card component as the form container
       - Fields: email (Input, type="email", required), password (Input, type="password", required, minLength 8), confirm password (Input, type="password", required)
       - **T&C Checkbox (CRITICAL):**
         - Use Checkbox component from shadcn/ui
         - Default: **UNCHECKED** (GDPR compliant -- no pre-checked boxes)
         - Label: 'I accept the [Terms and Conditions](/terms)' -- "Terms and Conditions" is a link that opens in a new tab
         - Registration button is DISABLED until checkbox is checked
         - Submit handler checks `hasAcceptedTerms` and throws error if false (defense in depth)
       - Password validation: passwords must match, minimum 8 characters
       - Submit calls `useAuthActions().register(email, password, hasAcceptedTerms)`
       - Show error messages for: mismatched passwords, email already exists, T&C not accepted
       - Include loading state on button during registration
       - Link to `/login` for existing users: "Already have an account? Sign in"
       - Same layout as Login (centered card, mobile-first)

    3. Create `src/pages/Terms.tsx`:
       - Simple page with placeholder T&C text
       - Use Card component for the content container
       - Include sections: "Terms of Service", "Privacy Policy", "Data Collection", "Your Rights"
       - Placeholder text explaining: what data is collected (energy logs, lifestyle profile), why (to provide personalized energy tracking), how it's stored (locally on device in IndexedDB), user rights (delete account, export data)
       - Back button or link to return to `/register`
       - Note: "This is placeholder text. Real legal text to be written."

    4. Verify all three pages compile: `npm run build`
  </action>
  <verify>
    - `npm run build` succeeds
    - `src/pages/Login.tsx` renders email/password form with signIn integration
    - `src/pages/Register.tsx` renders registration form with T&C checkbox (unchecked by default)
    - `src/pages/Register.tsx` disables submit button when checkbox unchecked
    - `src/pages/Terms.tsx` renders placeholder T&C text
    - All pages use shadcn/ui components (Card, Input, Button, Checkbox, Label)
    - All pages use design token colors
  </verify>
  <done>Login page authenticates via AuthContext. Register page enforces T&C acceptance (checkbox unchecked by default, button disabled without it, GDPR compliant). Terms page shows placeholder legal text. All pages are mobile-first, use design tokens, and handle error states.</done>
</task>

<task type="auto">
  <name>Task 2: Create app shell layout, protected routing, and integrate into App.tsx</name>
  <files>
    src/routes/ProtectedRoute.tsx
    src/routes/router.tsx
    src/components/layout/AppShell.tsx
    src/components/layout/Header.tsx
    src/pages/Dashboard.tsx
    src/App.tsx
    src/main.tsx
  </files>
  <action>
    1. Create `src/routes/ProtectedRoute.tsx`:
       ```typescript
       import { Navigate, Outlet } from 'react-router-dom';
       import { useAuthState } from '../contexts/AuthContext';

       export function ProtectedRoute() {
         const { isSignedIn, isLoading } = useAuthState();

         if (isLoading) {
           return (
             <div className="min-h-screen flex items-center justify-center bg-background">
               <div className="text-muted-foreground">Loading...</div>
             </div>
           );
         }

         return isSignedIn ? <Outlet /> : <Navigate to="/login" replace />;
       }
       ```

    2. Create `src/components/layout/Header.tsx`:
       - App title "Vector" on the left (text-lg font-bold, using primary color)
       - Right side: dark mode toggle button (icon: sun/moon, uses `useDarkMode` hook), logout button (Button variant="ghost", calls `useAuthActions().signOut`)
       - Use a simple header element with flex layout, padding, border-bottom
       - Mobile-friendly: horizontal padding, reasonable touch targets (min 44px)
       - Dark mode toggle: show sun icon when dark, moon icon when light (use simple SVG or unicode)
       - Sticky header: `sticky top-0 z-50 bg-background/95 backdrop-blur`

    3. Create `src/components/layout/AppShell.tsx`:
       - Wraps Header + Outlet (for nested routes)
       - Structure: `<div className="min-h-screen bg-background"><Header /><main className="container mx-auto px-4 py-6"><Outlet /></main></div>`
       - This is the layout for all authenticated pages

    4. Create `src/pages/Dashboard.tsx`:
       - Placeholder dashboard page that will be expanded in later phases
       - Show a welcome message: "Welcome to Vector, {user.email}" using `useAuthState()`
       - Show a Card with placeholder text: "Your energy dashboard will appear here. Start tracking in Phase 3."
       - Include placeholder sections for: Physical Energy, Mental Energy, Emotional Energy (using Card components)
       - Each section has a title and "Coming soon" text
       - Mobile-first: single column on mobile, multi-column on larger screens

    5. Create `src/routes/router.tsx` with React Router v7:
       ```typescript
       import { createBrowserRouter } from 'react-router-dom';
       import { ProtectedRoute } from './ProtectedRoute';
       import { AppShell } from '../components/layout/AppShell';
       import Login from '../pages/Login';
       import Register from '../pages/Register';
       import Terms from '../pages/Terms';
       import Dashboard from '../pages/Dashboard';

       export const router = createBrowserRouter([
         // Public routes
         { path: '/login', element: <Login /> },
         { path: '/register', element: <Register /> },
         { path: '/terms', element: <Terms /> },
         // Protected routes
         {
           element: <ProtectedRoute />,
           children: [
             {
               element: <AppShell />,
               children: [
                 { path: '/', element: <Dashboard /> },
               ],
             },
           ],
         },
       ]);
       ```

    6. Update `src/App.tsx` to use AuthProvider and RouterProvider:
       ```typescript
       import { RouterProvider } from 'react-router-dom';
       import { AuthProvider } from './contexts/AuthContext';
       import { router } from './routes/router';

       function App() {
         return (
           <AuthProvider>
             <RouterProvider router={router} />
           </AuthProvider>
         );
       }

       export default App;
       ```

    7. Ensure `src/main.tsx` still has the service worker registration and renders App.

    8. Run `npm run build` and verify no errors.

    9. Start dev server with `npm run dev` and verify:
       - Visiting `/` redirects to `/login` (not signed in)
       - Login page renders with form
       - Can navigate to `/register`
       - Can navigate to `/terms`
       - After registering (with T&C checked): redirected to `/` showing Dashboard
       - Dark mode toggle in header switches theme
       - Logout button returns to login page
  </action>
  <verify>
    - `npm run build` succeeds
    - `npm run dev` starts without errors
    - Visiting `/` when not signed in redirects to `/login`
    - `/login` shows login form
    - `/register` shows registration form with T&C checkbox
    - `/terms` shows T&C page
    - After successful login: `/` shows Dashboard inside AppShell
    - Header shows "Vector" title, dark mode toggle, logout button
    - Dark mode toggle switches classes and persists to localStorage
    - Logout clears session and redirects to `/login`
  </verify>
  <done>Protected routing redirects unauthenticated users to /login. AppShell wraps authenticated pages with Header (dark mode toggle + logout). Dashboard placeholder shows welcome message. Full auth flow works: register (with T&C) -> login -> dashboard -> logout -> login wall.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete Phase 1 auth flow and PWA functionality</name>
  <files>src/App.tsx</files>
  <action>
    Human verification checkpoint. What was built: Complete PWA with authentication flow, design system, dark mode, and offline shell support covering AUTH-01..05, PWA-01..03, DESIGN-01..05.

    Steps for the user to verify:

    1. Auth flow (AUTH-01..05):
       - Visit the app -- you should see ONLY the login page (no content accessible)
       - Click "Register" link
       - Try submitting without checking T&C -- button should be disabled
       - Check the T&C checkbox, fill in email/password, register
       - Verify you're redirected to the Dashboard
       - Close the browser tab, reopen -- you should still be logged in (session persistence)
       - Click "Logout" in the header -- you should return to the login page
       - Log back in with the same credentials

    2. PWA (PWA-01..03):
       - Run `npm run build && npx serve dist` (or use the dev server)
       - Open Chrome DevTools > Application > Manifest -- verify manifest loads
       - Open Chrome DevTools > Application > Service Workers -- verify SW is registered
       - Run Lighthouse PWA audit
       - On mobile: verify "Add to Home Screen" is available

    3. Design System (DESIGN-01..05):
       - Toggle dark mode via the header button -- theme should switch
       - Reload the page -- dark mode preference should persist
       - Check that colors use calming blues/greens
       - Verify mobile-first layout and consistent component styling

    4. Offline (PWA-03):
       - Build and serve: `npm run build && npx serve dist`
       - Load the app, go offline (DevTools > Network > Offline)
       - Reload -- the app shell should still load

    Resume signal: Type "approved" if all checks pass, or describe any issues found.
  </action>
  <verify>User confirms all Phase 1 requirements pass visual and functional inspection.</verify>
  <done>All Phase 1 success criteria confirmed by user: installable PWA, login wall, T&C enforcement, session persistence, offline shell, dark mode toggle with persistence, neuroscience-based design.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds
2. Auth flow: register with T&C -> login -> session persists -> logout -> login wall
3. Protected routes: `/` redirects to `/login` when not authenticated
4. Dark mode toggle works and persists preference
5. PWA manifest generated with correct metadata
6. Service worker registered and caches app shell
7. App shell loads offline
8. Mobile-first responsive layout
9. Consistent shadcn/ui component styling throughout
</verification>

<success_criteria>
- Complete auth flow works end-to-end (register, T&C, login, persist, logout)
- Login wall: no content accessible without authentication
- Dark mode toggle with persistence
- PWA installable with service worker
- Offline shell loads
- Mobile-first responsive design
- All Phase 1 requirements (AUTH-01..05, PWA-01..03, DESIGN-01..05) are met
</success_criteria>

<output>
After completion, create `.planning/phases/01-pwa-shell-auth-design-system/01-04-SUMMARY.md`
</output>
