---
phase: 01-pwa-shell-auth-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/styles/theme.css
  - src/index.css
  - src/lib/utils.ts
  - src/lib/useDarkMode.ts
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/card.tsx
  - src/components/ui/checkbox.tsx
  - src/components/ui/label.tsx
  - src/components/ui/separator.tsx
  - components.json
autonomous: true

must_haves:
  truths:
    - "Neuroscience color tokens are defined as CSS custom properties (calm blues/greens, energy oranges/yellows, stress pink)"
    - "Dark mode class toggles all color tokens to reduced-luminance variants"
    - "useDarkMode hook reads from localStorage and system preference, persists choice"
    - "shadcn/ui Button, Input, Card, Checkbox, Label components are installed and styled with custom tokens"
    - "Dark mode flash is prevented on page load"
  artifacts:
    - path: "src/styles/theme.css"
      provides: "Neuroscience color tokens as CSS custom properties for light and dark modes"
      contains: "color-calm-blue"
    - path: "src/lib/useDarkMode.ts"
      provides: "Dark mode toggle hook with localStorage persistence"
      exports: ["useDarkMode"]
    - path: "src/index.css"
      provides: "Tailwind imports and theme integration"
      contains: "tailwindcss"
    - path: "src/components/ui/button.tsx"
      provides: "shadcn/ui Button component"
    - path: "src/components/ui/input.tsx"
      provides: "shadcn/ui Input component"
    - path: "src/components/ui/card.tsx"
      provides: "shadcn/ui Card component"
  key_links:
    - from: "src/styles/theme.css"
      to: "src/index.css"
      via: "CSS import"
      pattern: "@import.*theme"
    - from: "src/lib/useDarkMode.ts"
      to: "document.documentElement"
      via: "classList.add/remove('dark')"
      pattern: "classList\\.(add|remove).*dark"
---

<objective>
Create the neuroscience-based design system with color tokens, dark mode support, and core UI components that all future phases will use for consistent styling.

Purpose: Establish the visual foundation -- neuroscience-informed colors for energy states (calming blues/greens for rest, warm tones for energy/alerts, pink for stress reduction) plus dark mode toggle with persisted preference. This ensures every component built in later phases uses the same design language.
Output: Theme CSS file with color tokens, dark mode hook, Tailwind configuration with custom colors, and shadcn/ui base components (Button, Input, Card, Checkbox, Label, Separator).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pwa-shell-auth-design-system/01-RESEARCH.md
@.planning/phases/01-pwa-shell-auth-design-system/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create neuroscience color tokens and configure Tailwind theme with dark mode</name>
  <files>
    src/styles/theme.css
    src/index.css
    src/lib/utils.ts
  </files>
  <action>
    1. Create `src/styles/theme.css` with CSS custom properties for the neuroscience-based color palette. Use HSL format for Tailwind compatibility. Include comments citing the neuroscience rationale:

       ```css
       /*
        * Vector Neuroscience Color System
        *
        * Based on color psychology research:
        * - Blues: Trust, focus, calm (reduces cortisol) -- used for rest/recovery states
        * - Greens: Restorative, reduces anxiety -- used for balance/health indicators
        * - Teals: Balance, peace -- used for neutral/positive states
        * - Oranges: Energy, motivation, dopamine-associated -- used for energy/alert states
        * - Yellows: Attention, optimism -- used for warnings/highlights
        * - Pinks: Sedative effect, reduces aggression (Baker-Miller pink) -- used for stress indicators
        *
        * Sources: mockflow.com/blog/color-psychology-in-ui-design, neurodesign research
        */

       @layer base {
         :root {
           /* Calm palette -- rest, recovery, trust, focus */
           --color-calm-blue: 217 91% 60%;
           --color-calm-green: 142 71% 45%;
           --color-calm-teal: 174 62% 47%;

           /* Energy palette -- alerts, productivity, motivation */
           --color-energy-orange: 24 100% 50%;
           --color-energy-yellow: 45 100% 51%;

           /* Stress reduction */
           --color-stress-pink: 350 100% 88%;

           /* UI Base - light mode */
           --color-background: 0 0% 100%;
           --color-foreground: 222 47% 11%;
           --color-card: 210 40% 98%;
           --color-card-foreground: 222 47% 11%;
           --color-popover: 0 0% 100%;
           --color-popover-foreground: 222 47% 11%;
           --color-primary: 217 91% 60%;
           --color-primary-foreground: 0 0% 100%;
           --color-secondary: 210 40% 96%;
           --color-secondary-foreground: 222 47% 11%;
           --color-muted: 210 40% 96%;
           --color-muted-foreground: 215 16% 47%;
           --color-accent: 174 62% 47%;
           --color-accent-foreground: 0 0% 100%;
           --color-destructive: 0 84% 60%;
           --color-destructive-foreground: 0 0% 100%;
           --color-border: 214 32% 91%;
           --color-input: 214 32% 91%;
           --color-ring: 217 91% 60%;
           --radius: 0.5rem;
         }

         .dark {
           /* Calm palette -- reduced luminance for dark mode */
           --color-calm-blue: 217 91% 45%;
           --color-calm-green: 142 71% 35%;
           --color-calm-teal: 174 62% 37%;

           /* Energy palette -- reduced luminance */
           --color-energy-orange: 24 100% 40%;
           --color-energy-yellow: 45 100% 41%;

           /* Stress reduction -- reduced luminance */
           --color-stress-pink: 350 100% 78%;

           /* UI Base - dark mode */
           --color-background: 222 47% 8%;
           --color-foreground: 210 40% 98%;
           --color-card: 222 47% 11%;
           --color-card-foreground: 210 40% 98%;
           --color-popover: 222 47% 11%;
           --color-popover-foreground: 210 40% 98%;
           --color-primary: 217 91% 55%;
           --color-primary-foreground: 0 0% 100%;
           --color-secondary: 217 33% 17%;
           --color-secondary-foreground: 210 40% 98%;
           --color-muted: 217 33% 17%;
           --color-muted-foreground: 215 20% 65%;
           --color-accent: 174 62% 37%;
           --color-accent-foreground: 0 0% 100%;
           --color-destructive: 0 63% 31%;
           --color-destructive-foreground: 0 0% 100%;
           --color-border: 217 33% 22%;
           --color-input: 217 33% 22%;
           --color-ring: 217 91% 55%;
         }
       }
       ```

    2. Update `src/index.css` to import Tailwind and the theme. For Tailwind CSS v4 with the Vite plugin, the CSS file should use:
       ```css
       @import "tailwindcss";
       @import "./styles/theme.css";

       /* Base styles */
       @layer base {
         * {
           @apply border-border;
         }
         body {
           @apply bg-background text-foreground;
           font-feature-settings: "rlig" 1, "calt" 1;
         }
       }
       ```

       Note: Tailwind v4 uses `@import "tailwindcss"` instead of the old `@tailwind base; @tailwind components; @tailwind utilities;` directives.

    3. Ensure `src/lib/utils.ts` exists with the shadcn/ui `cn()` utility (this may already exist from shadcn init):
       ```typescript
       import { type ClassValue, clsx } from 'clsx';
       import { twMerge } from 'tailwind-merge';

       export function cn(...inputs: ClassValue[]) {
         return twMerge(clsx(inputs));
       }
       ```

    4. Verify Tailwind CSS v4 processes the theme correctly. With the Vite plugin approach, Tailwind v4 auto-detects CSS custom properties and makes them available as utilities. If additional configuration is needed (e.g., in a `tailwind.config.ts` or CSS `@theme` directive), add the custom color mappings:
       ```css
       /* In src/index.css, after the @import */
       @theme {
         --color-calm-blue: hsl(var(--color-calm-blue));
         --color-calm-green: hsl(var(--color-calm-green));
         --color-calm-teal: hsl(var(--color-calm-teal));
         --color-energy-orange: hsl(var(--color-energy-orange));
         --color-energy-yellow: hsl(var(--color-energy-yellow));
         --color-stress-pink: hsl(var(--color-stress-pink));
         --color-background: hsl(var(--color-background));
         --color-foreground: hsl(var(--color-foreground));
         --color-card: hsl(var(--color-card));
         --color-card-foreground: hsl(var(--color-card-foreground));
         --color-primary: hsl(var(--color-primary));
         --color-primary-foreground: hsl(var(--color-primary-foreground));
         --color-secondary: hsl(var(--color-secondary));
         --color-secondary-foreground: hsl(var(--color-secondary-foreground));
         --color-muted: hsl(var(--color-muted));
         --color-muted-foreground: hsl(var(--color-muted-foreground));
         --color-accent: hsl(var(--color-accent));
         --color-accent-foreground: hsl(var(--color-accent-foreground));
         --color-destructive: hsl(var(--color-destructive));
         --color-destructive-foreground: hsl(var(--color-destructive-foreground));
         --color-border: hsl(var(--color-border));
         --color-input: hsl(var(--color-input));
         --color-ring: hsl(var(--color-ring));
         --radius: var(--radius);
       }
       ```

       Tailwind v4 uses the `@theme` directive in CSS instead of `tailwind.config.ts` for theme extension. Verify this approach works by checking that `bg-background`, `text-foreground`, `bg-calm-blue`, etc. classes are recognized.

    5. Run `npm run build` to verify no CSS/Tailwind errors.
  </action>
  <verify>
    - `npm run build` succeeds without CSS or Tailwind errors
    - `src/styles/theme.css` defines all neuroscience color tokens for both `:root` and `.dark`
    - `src/index.css` imports tailwindcss and theme.css
    - Custom color classes (bg-background, text-foreground, bg-primary, etc.) work in built output
  </verify>
  <done>Neuroscience color palette is fully defined as CSS custom properties with light and dark variants. Tailwind is configured to use these tokens. All UI base colors (background, foreground, card, primary, secondary, muted, accent, destructive, border) use the token system.</done>
</task>

<task type="auto">
  <name>Task 2: Create dark mode hook and install shadcn/ui base components</name>
  <files>
    src/lib/useDarkMode.ts
    src/components/ui/button.tsx
    src/components/ui/input.tsx
    src/components/ui/card.tsx
    src/components/ui/checkbox.tsx
    src/components/ui/label.tsx
    src/components/ui/separator.tsx
    components.json
  </files>
  <action>
    1. Create `src/lib/useDarkMode.ts` -- a React hook for dark mode toggle with localStorage persistence and system preference detection:
       ```typescript
       import { useEffect, useState, useCallback } from 'react';

       export function useDarkMode() {
         const [isDark, setIsDark] = useState(() => {
           if (typeof window === 'undefined') return false;
           const stored = localStorage.getItem('theme');
           if (stored) return stored === 'dark';
           return window.matchMedia('(prefers-color-scheme: dark)').matches;
         });

         useEffect(() => {
           const root = document.documentElement;
           if (isDark) {
             root.classList.add('dark');
             localStorage.setItem('theme', 'dark');
           } else {
             root.classList.remove('dark');
             localStorage.setItem('theme', 'light');
           }
         }, [isDark]);

         const toggleDark = useCallback(() => {
           setIsDark(prev => !prev);
         }, []);

         return { isDark, toggleDark, setIsDark } as const;
       }
       ```

    2. Install shadcn/ui components. First ensure `components.json` exists (created during `npx shadcn@latest init` in Plan 01). Then add components:
       ```bash
       npx shadcn@latest add button input card checkbox label separator
       ```

       This creates files in `src/components/ui/`. Each component uses the Tailwind tokens from our theme. If shadcn init was not run in Plan 01, run it first:
       ```bash
       npx shadcn@latest init --defaults
       npx shadcn@latest add button input card checkbox label separator
       ```

    3. Verify each component file exists and imports from the correct paths:
       - `src/components/ui/button.tsx` -- exports Button with variants (default, destructive, outline, secondary, ghost, link)
       - `src/components/ui/input.tsx` -- exports Input with proper styling
       - `src/components/ui/card.tsx` -- exports Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter
       - `src/components/ui/checkbox.tsx` -- exports Checkbox (used for T&C acceptance)
       - `src/components/ui/label.tsx` -- exports Label
       - `src/components/ui/separator.tsx` -- exports Separator

    4. Verify that all components use the design token CSS variables (not hardcoded colors). The shadcn/ui components should reference `bg-primary`, `text-primary-foreground`, `bg-card`, etc., which map to our neuroscience tokens.

    5. Run `npm run build` to verify all components compile correctly.
  </action>
  <verify>
    - `npm run build` succeeds
    - `src/lib/useDarkMode.ts` exports `useDarkMode`
    - All 6 shadcn/ui component files exist in `src/components/ui/`
    - Components use Tailwind token classes (bg-primary, text-foreground, etc.)
    - No TypeScript errors
  </verify>
  <done>Dark mode hook is implemented with localStorage persistence and system preference fallback. Six shadcn/ui components (Button, Input, Card, Checkbox, Label, Separator) are installed and styled with the neuroscience color token system. All components respect dark mode via the CSS custom property system.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `src/styles/theme.css` contains neuroscience color tokens (calm-blue, calm-green, energy-orange, energy-yellow, stress-pink) in both light and dark modes
3. `src/lib/useDarkMode.ts` reads from localStorage, falls back to system preference, persists changes
4. All 6 shadcn/ui components exist and compile
5. Tailwind custom colors are available (bg-calm-blue, bg-energy-orange, etc.)
6. Dark mode class toggles all CSS custom properties
</verification>

<success_criteria>
- Neuroscience color tokens documented and defined in theme.css
- Dark mode toggle hook works with persistence
- Six base UI components installed and styled with tokens
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-pwa-shell-auth-design-system/01-02-SUMMARY.md`
</output>
